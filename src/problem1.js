function problem1(pobi, crong) {
  var answer;

  return answer;
}

function sum(num) {
  return num
    .toString()
    .split('')
    .map(Number)
    .reduce((acc, cur) => acc + cur, 0);
}

module.exports = problem1;
