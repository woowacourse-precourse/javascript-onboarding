function problem1(pobi, crong) {
  var answer;
  return answer;
const sumOfEachDigit = (num) => {
  let sum = 0
  while (num > 0) {
    let digit = num % 10
    num = (num - digit) / 10
    sum += digit
  }
  return sum
}
}

module.exports = problem1;
