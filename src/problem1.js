function problem1(pobi, crong) {
  var answer;
  return answer;
}

const addPage = (page) => {
  return page
    .toString()
    .split("")
    .reduce((prev, cur) => prev + +cur, 0);
};

module.exports = problem1;
