function makeSum(n) {
  let val = 0;
  for (let i = 0; i < n.length; i++) {
    val += parseInt(n[i]);
  }
}

function makeMul(n) {
  let val = 0;
  for (let i = 0; i < n.length; i++) {
    val *= parseInt(n[i]);
  }
}

function problem1(pobi, crong) {
  var answer;
  return answer;
}

module.exports = problem1;
