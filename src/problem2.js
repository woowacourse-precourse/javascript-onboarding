/**
 * 기능 목록
 * 1. 문자열을 입력 받는다
 * 2. 문자열을 removedIndex부터 (문자열의 길이 - 1) 만큼 순회한다.
 *   2-1. i번째 글자와 (i + 1)번째 글자가 같으면 두 글자 제거한다.
 *     2-1-1. i가 0이면 removedIndex를 0으로 변경한다.
 *     2-1-2. i가 0이 아니면 i-- 후 removedIndex를 i로 변경한다.
 * 3-1. isRemoved가 true이면 2번으로
 * 3-2. isRemoved가 false이면 문자열 결과를 return한다 (종료)
 *
 * @param {string} cryptogram
 * @returns {string} result
 */
function problem2(cryptogram) {
  var answer;
  return answer;
}

module.exports = problem2;
