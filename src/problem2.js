/* 
1. 주어진 문자열을 문자 별로 자르고 배열에 저장
2. 문자들이 담긴 배열을 반복문을 통해 연속된 중복 문자들을 제거
3. 삭제한 결과를 다시 하나의 문자열로 만듦
4. 삭제 후 합친 문자열 return
*/

function problem2(cryptogram) {
  var answer;

  return answer;
}

module.exports = problem2;
