function problem2(cryptogram) {
  var answer;
  return answer;
}

module.exports = problem2;

// 입력값 : 문자열

// 구현할 기능
// 중복되는 값이 있는지 판별하는 함수
// 중복되는 값 삭제하는 함수

// 구현 방향성
// 중복되는 값이 없을 때까지 반복하는 반복문을 작성한다.
// 중복되는 값을 찾아 모두 삭제한다.
// 정규표현식 활용해보기!
