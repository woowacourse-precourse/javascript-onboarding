function problem2(cryptogram) {
  var answer;
  if (crypto.length <= 100 && crypto.length > 1) {
    if (crypto === crypto.toLowerCase()) {
    } else {
      return null;
    }
  } else {
    return null;
  }
  return answer;
}

module.exports = problem2;
