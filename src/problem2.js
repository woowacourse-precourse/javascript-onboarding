function problem2(cryptogram) {
  var answer;

  // empty stack 선언
  var stack = [];
  let arr = [...cryptogram]; // 나머지 배열 할당

  // stack에 하나씩 push

  // 연속하는 중복 문자 있으면 pop

  // 중복 없을 때까지 반복

  return answer;
}

module.exports = problem2;
