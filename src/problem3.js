/**
 * splitNumberToDigits
 * @type {(n: number) => number[]}
 */

/**
 * range
 * @type {(n: number) => number[]}
 */
const range = (n) => [...Array(n).keys()];

function problem3(number) {
  var answer;
  return answer;
}

module.exports = problem3;
