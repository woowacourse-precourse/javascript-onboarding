function problem3(number) {
  var answer;
  return answer;

  //1. 숫자를 배열로 변환해 값에 [3,6,9]가 몇 개 들어가는지 반환하는 함수 작성.
  //2. 반복문을 사용해 3부터 number까지 기능1 함수 동작하도록 작성.
  //3. 반환값들을 전부 더해 리턴.
}

module.exports = problem3;
