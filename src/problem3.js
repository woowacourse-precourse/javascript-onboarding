function find369(number) {
  if (number === "3" || number === "6" || number === "9") return true;
  else return false;
}

function problem3(number) {
  var answer;

  // number를 문자열 배열로 변환하는 기능
  number = String(number).split("");

  return answer;
}

module.exports = problem3;
