function problem3(number) {
  let answer = 0;
  const RULE = "369";

  let counter = 0;
  while (counter <= number) {
    String(counter).split("").forEach(x => {

    })
  }
}

module.exports = problem3;
