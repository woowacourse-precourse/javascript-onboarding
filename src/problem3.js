function problem3(number) {
  let answer = 0;
  [...Array(1,number + 1).keys()].forEach(
    (num) =>  [...String(ele)].forEach(
      (ele) => {
      }
    )
  );
  
  return answer;
  
}

module.exports = problem3;

