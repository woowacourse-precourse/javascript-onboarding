/*
  기능목록
  1. number로 입력된 숫자를 문자열로 변환한다.
  2. 문자열의 각 문자에 3, 6, 9가 있는지 확인한다.
  3. 3, 6, 9가 있으면 clap을 추가한다.
*/ 
function problem3(number) {
  var answer;
  return answer;
}

module.exports = problem3;
