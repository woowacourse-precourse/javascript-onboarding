// number의 각 자리 숫자를 구해 문자열 배열로 저장하는 기능
const convertToDigitArr = (number) => {
  number = number.toString();
  return number;
};

function problem3(number) {
  var answer;
  return answer;
}

module.exports = problem3;
