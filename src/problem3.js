function problem3(number) {
  const clap = 0
  for(i=1; i<=number;i++){
    //기능 1. 해당 숫자가 3 또는 6 또는 9를 포함한다면 그 숫자만큼 clap 추가
    if(i.includes(3 || 6 || 9)){
      clap += i
    }
    else{
    }
  }

  var answer=clap;
  return answer;
}

module.exports = problem3;
