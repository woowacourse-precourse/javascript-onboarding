function problem3(number) {
  var answer;

  return answer;
}

// 숫자 자리수로 나누기
function makeNumDigits(num) {
  num = num.toString();
  let array = [];
  for (let i = 0; i < num.length; i++) {
    array.push(num[i]);
  }
  return array;
}

module.exports = problem3;
