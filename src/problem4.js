function problem4(word) {
  let result = "";
  for (let i = 0; i < word.length; i++) {
    if (word[i] === " ") {
      result += " ";
      continue;
    }
    let ascii = word.charCodeAt([i]);
  }
}

module.exports = problem4;
