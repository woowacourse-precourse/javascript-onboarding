//기능 목록 작성
//1. 알파벳을 한 글자마다 배열로 저장한다.
//1. 알파벳을 숫자로 치환해서 word값을 0붜 25까지의 숫자로 바꾼다.
//2. 25에 바뀐 숫자들을 빼서 저장한다.
//3. 바뀐 값을 알파벳으로 다시 바꿔서 result에 저장한다.
//4. result를 return한다.

function problem4(word) {
    var answer;
    let tmpword = word;
    let tmpArray = tmpword.split("");

    return answer;
}

module.exports = problem4;