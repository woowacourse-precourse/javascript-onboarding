// 알파벳인지 확인하는 함수
function checkAlphabet(str) {
  const regExp = /[a-zA-Z]/g;
  if(regExp.test(str)){
    return true;
  } else{
    return false;
  }
}

// main 함수
function problem4(word) {
  let answer;
  return answer;
}

module.exports = problem4;
