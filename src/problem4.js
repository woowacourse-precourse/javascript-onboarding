function problem4(word) {
  var answer;
  // 문자열을 문자로 split

  // 변환된 문자열을 붙이기

  return answer;
}

// ASCII를 사용하여 문자 변환하는 함수
function changeWord(word) {
  // 대문자 (65~90)
  // 소문자 (97~122)
  // 예외처리 (띄어쓰기)
}

module.exports = problem4;
