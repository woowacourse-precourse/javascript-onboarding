/*
 * 기능 분석
 * [x] 알파벳을 확인하고 선택하는 정규표현식 작성
 * [] 알파벳을 청개구리 사전에 맞게 변경하는 기능
 * [] 청개구리사전식으로 변경된 문자열로 반환하는 기능
 */

const ALPHABETICAL_REG_EXP = /([A-Z])|([a-z])/g;

function problem4(word) {
  var answer;
  return answer;
}

module.exports = problem4;
