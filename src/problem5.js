function problem5(money) {
  var answer;
  // 화폐 단위 배열 선언
  wonType = [50000, 10000, 5000, 1000, 500, 100, 50, 10, 1];
  let count = 0;

  // 화폐 큰 단위 순서대로 나누고 나머지 넘기기

  // 각 화폐당 변환 갯수 배열에 담기
  return answer;
}

module.exports = problem5;
