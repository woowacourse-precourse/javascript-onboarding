function problem5(money) {
  var answer;
  return answer;
}

module.exports = problem5;

const calcCharge = (mopny) => {
  const unit = [50000, 10000, 5000, 1000, 500, 100, 50, 10, 1];
};

/**
 * [50000, 10000, 5000, 1000, 500, 100, 50, 10, 1]
 * 1. 값을 나눈 몫이 0이 아닌 경우, 계산 후 저장 및 값 빼기.
 * 2. 반복
 *
 *
 *
 */
