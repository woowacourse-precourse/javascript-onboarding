/* 
  1. 화폐 단위 배열 생성
  2. 생성한 화폐 단위 배열을 순회하면서 입력된 money를 나눈다.
  3. 2에서 나눈 몫만큼 money를 뺀다.
*/
function problem5(money) {
  const moneyUnit = [50000, 10000, 5000, 1000, 500, 100, 50, 10, 1];
  var moneyCount = [];
  let idx = 0;
}

module.exports = problem5;
