function problem6(forms) {
  var answer;
  return answer;
}

// 1. 닉네임을 이메일과 분리하는 기능
function nickSplit(id) {}
// 2. 같은 글자가 연속적으로 포함하는 닉네임을 검사하는 기능
function checkTwoChar(nickName) {}
// 3. 이메일 붙이는 기능
function emailConcat(id) {}

module.exports = problem6;
