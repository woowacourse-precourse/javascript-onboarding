function problem6(forms) {
  var answer;

  // 1. 닉네임 리스트를 돌면서 두 글자씩 뽑아서 리스트를 만든 후 중복 제거

  // 2. 두 글자 리스트를 돌면서 해당하는 이메일들을 얻음

  // 3. 같은 글자가 연속적으로 포함된 닉네임을 가진 이메일을 뽑음
  
  // 4. 오름차순 정렬 후 중복 제거
  
  return answer;
}

module.exports = problem6;
