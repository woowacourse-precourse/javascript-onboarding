// 기능 요구 사항

// -[] 2글자씩 잘라서 중복배열에 저장하기
// -[] 중복배열에 해당하는 이름 검색하기
// - [] 검색된 중복 이름을 저장하기
// - [] 담긴 이름 오릋마순으로 정렬
// - [] 중복 제거

function problem6(forms) {
  var answer;
  return answer;
}

module.exports = problem6;
