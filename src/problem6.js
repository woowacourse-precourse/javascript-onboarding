// 의사 코드 작성
// 1. 2차원 배열인 forms 인자의 원소들을 순회
// 2. 이름 한 개를 기준으로 나머지 이름을 전부 탐색
// 3. 같은 글자가 연속적으로 포함되면 결과 배열에 이메일 push
// 4. Set 객체를 사용해서 중복 제거
// 5. 결과 배열의 이메일들을 오름차순으로 정렬

function problem6(forms) {
  var answer;
  return answer;
}

module.exports = problem6;
