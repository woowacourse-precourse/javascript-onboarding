//기능 목록 작성
//1. 닉네임을 두개씩 구조체로 저장한다. ex) {이름: 제이엠, 이메일:jm@email.com, 문자들 = ["제이", "이엠"]}
//2. 문자들을 닉네임 단위로 모두 비교해서 같은 값이 있으면 이메일을 반환한다.
//3. 이메일들을 전부 배열로 저장한 다음 중복값을 제거한다.
//4. 중복값을 제거한 배열을 반환한다.

function problem6(forms) {
  var answer;
  return answer;
}

module.exports = problem6;
