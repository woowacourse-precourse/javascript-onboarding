function problem6(forms) {
  const getNickname = (arr) => {
    return arr.map((item) => {
      return item[1];
    });
  };

  const getEmail = (arr) => {
    return arr.map((item) => {
      return item[0];
    });
  };

  let answer;
  return answer;
}

module.exports = problem6;
