// 1. [ ] key: value | 닉네임 : email 형식의 map 만들기
// 2. 같은 글자가 연속으로 포함되는 닉네임을 판별하는 로직
//    2.1 [ ] key를 가져와서 두글자씩 자른다
//    2.2 [ ] 다른 key들이 해당 글자를 포함하는지 검사한다 (함수 작성)
//        Q. 어떻게 검사할 것인가? -> 정규표현식으로 해당 글자를 포함하는지 검사
//    2.3 [ ] 해당 글자를 포함하면 answer에 value를 추가한다 
// 3. answer를 정렬해준다

function problem6(forms) {
  var answer;
  return answer;
}

module.exports = problem6;
