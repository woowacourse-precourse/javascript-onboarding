function problem7(user, friends, visitors) {
  var answer;
  return answer;
}

// 함께 아는 친구수 count 함수
function withFriends(friend) {}

// 타임라인 방문수 count 함수
function visitFriends(friend) {}

module.exports = problem7;
